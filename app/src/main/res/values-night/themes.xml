<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.SpeedMath" parent="Theme.MaterialComponents.DayNight.DarkActionBar">

        <!-- DARK MODE COLORS -->
        <item name="android:colorBackground">@color/grey_dark_1</item>
        <item name="android:textColor">@color/white</item>

        <item name="colorPrimary">@color/blue_primary</item>
        <item name="colorPrimaryDark">@color/blue_primary_dark</item>
        <item name="colorAccent">@color/blue_primary</item>

        <!-- NAV + STATUS BAR -->
        <item name="android:navigationBarColor">@color/black</item>
        <item name="android:statusBarColor">@color/black</item>

        <!-- FONT -->
        <item name="android:fontFamily">@font/inter_regular</item>

    </style>

</resources>
